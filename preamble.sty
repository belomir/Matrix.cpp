% report preamble
% © 2017 Sergey Roganov <sergey@roganof.ru>
% GNU/GPL v.3+

\ProvidesPackage{preamble}

\usepackage{ucs} % кодировка исходника
\usepackage[utf8]{inputenc} % кодировка исходника
\usepackage[T2A]{fontenc}
\usepackage[russian]{babel} % локализация, переносы для последнего языка
% \usepackage[a4paper,left=30mm,right=15mm,top=20mm,bottom=20mm,headsep=5mm,includeheadfoot]{geometry} % поля
\usepackage[a4paper,left=30mm,right=15mm,top=20mm,bottom=20mm]{geometry} % поля
\usepackage[pdfencoding=unicode,pdfusetitle]{hyperref} % ссылки и кирилличская метаинформация в pdf
\usepackage{cmap} % возможность кирилличского поиска по pdf
\makeatletter
\hypersetup{
	unicode=true,
	pdfauthor={\@author},
	pdftitle={\@title},
	pdfborder=0 0 0,
%	linktocpage=true
% 	colorlinks,
%     citecolor=black,
%     filecolor=black,
%     linkcolor=black,
%     urlcolor=black
}
\makeatother
\usepackage{latexsym}
\usepackage{amssymb}
\usepackage[sumlimits, intlimits]{amsmath}
\usepackage[warn]{mathtext} % русские буквы в форммулах
\usepackage[protrusion,babel,final,factor=1500]{microtype} % висячая пунктуация
\usepackage{indentfirst} % абзацные отступы для первого после заголовка абзаца
%\linespread{1.3} % полуторный интервал
\frenchspacing % пространство между предложениями и абзацами, как в европе
\parindent=1.25cm % размер красной строки
\clubpenalty=9900 % запрет начальных висячих строк
\widowpenalty=9900 % запрет концевых висячих строк
\def\labelitemi{—} % значок маркированных списков
\addto\captionsrussian{\def\contentsname{Содержание}} % вывод русского «Оглавление»
\usepackage{tocloft}                                    % точки в оглавлении
\renewcommand{\cftchapleader}{\cftdotfill{\cftdotsep}}  % глава (chapter)
\renewcommand{\cfttoctitlefont}{\Large\bfseries}
\renewcommand{\labelenumi}{\asbuk{enumi})}
\renewcommand\appendixname{Приложение} % название
\usepackage{titlesec} % настройка заголовков
\titleformat{\chapter}[display]{\bfseries\Large}{\thechapter}{0pt}{}[]
\titlespacing*{\chapter}{0pt}{0pt}{1em}[0pt]
\usepackage{xcolor}
\newcommand{\textcolordummy}[2]{#2}
\usepackage{listings}
\usepackage{tabularx}
\usepackage{embedfile}
\usepackage{bashful}
\lstset{
	numbers=left,
	numberstyle=\footnotesize\ttfamily\color{gray},
	numbersep=2em,
	tabsize=4,
	breaklines=true,
	showstringspaces=false,
	basicstyle=\footnotesize\ttfamily,
	commentstyle=\footnotesize\color{gray},
	keywordstyle=\color{blue}\bfseries\ttfamily,
	stringstyle=\color{red},
    inputencoding=utf8x,
	extendedchars=true,
	language=C++
}

\endinput
